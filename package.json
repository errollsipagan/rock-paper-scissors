{
  "name": "rock-paper-scissors",
  "version": "1.0.0",
  "description": "",
  "main": "./src/app.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only --project tsconfig.server.json src/server/app.ts",
    "dev:ws": "ts-node-dev --respawn --transpile-only --project tsconfig.server.json src/server/game.ts",
    "build:server": "tsc -p tsconfig.server.json",
    "build:client": "node ./scripts/build-client.js",
    "build": "npm run build:client && npm run build:server",
    "start": "cross-env NODE_ENV=production node dist/server/app.js",
    "lint": "eslint . --ext .ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/ws": "^8.18.1",
    "express": "^5.1.0",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.3.0",
    "esbuild": "^0.25.9",
    "nodemon": "^3.1.10",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.2"
  }
}
